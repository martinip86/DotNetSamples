# Sample Template 

## Features

*WebApi*
- Metrics @ `/metrics` endpoint via AppMetrics
- Health Check @ `/health`

*GrpcService*
- Metrics @ `/metrics` endpoint via AppMetrics
- Health Check @ `/health`